import { SubDescriptionPipe } from './sub-description.pipe';

describe('SubDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new SubDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});

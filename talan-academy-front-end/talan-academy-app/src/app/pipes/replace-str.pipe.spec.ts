import { ReplaceStrPipe } from './replace-str.pipe';

describe('ReplaceStrPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceStrPipe();
    expect(pipe).toBeTruthy();
  });
});
